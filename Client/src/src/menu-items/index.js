// project import
import pages from './pages';
import dashboard from './dashboard';
import allPages from './allPages';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, pages, allPages]
};

export default menuItems;
